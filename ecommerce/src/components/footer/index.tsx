const Footer = () => {
  return (
    <footer className="fixed bottom-0 left-0 w-full mt-1 bg-stone-800/90 text-white text-center py-3 text-sm">
      <p>
        <strong className="font-bold pr-2">Atenção!!!</strong>
        Este site é apenas um estudo de React.ts, sem a intenção de
        comercializar os produtos contidos nele.
      </p>
    </footer>
  );
};

export default Footer;
